import React, { FC } from "react"

import { NormalLayout } from "../../components/layouts"

import { PageProps } from "gatsby"

const MobileApps: FC<PageProps> = props => (
  <NormalLayout {...props}>MobileApps</NormalLayout>
)

export default MobileApps
