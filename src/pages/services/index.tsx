import React, { FC } from "react"

import { NormalLayout } from "../../components/layouts"

import { PageProps } from "gatsby"

const Services: FC<PageProps> = props => (
  <NormalLayout {...props}>Services</NormalLayout>
)

export default Services
